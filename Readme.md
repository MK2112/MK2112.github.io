# mk2112.github.io

This website won't run if accessed via `file://` protocol.<br>
It needs to be accessed via `http://` or `https://` protocol.

Run `python3 -m http.server 8000` in repo dir, then access `http://localhost:8000/` in browser.